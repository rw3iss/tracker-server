// Placeholder. Real values are stamped by bin/generate-dashboard-version.mjs
// during `npm run dashboard:bundle`, replacing this file in dist/dashboard/js/.
// In dev / source-served mode, the dashboard reads `'dev'` here and the
// auto-updater short-circuits without showing the modal.
window.__TRACKER_DASHBOARD_VERSION_INFO__ = {
  version:   'dev',
  sha:       null,
  builtAt:   null,
  repoUrl:   '',
  commits:   [],
  changelog: '',
};
